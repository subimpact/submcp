# Copy to .env.prod (gitignored) and fill in real values.
# Used by scripts/start-prod.sh — NEVER commit this file.
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=changeme
POSTGRES_PASSWORD=changeme
POSTGRES_DB=metamcp_db
